import "./calculator.css"

const Calculator = ({props}) => {
    return <div className="calculator">
        Hello this is a test to make sure im working    
        {props}
    </div>;
};

export default Calculator;